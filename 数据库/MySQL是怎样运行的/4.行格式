数据页
    将数据划分为若干个页，以页作为磁盘和内存之间交互的基本单位，InnoDB中页的大小一般为 16 KB
行格式
    COMPACT
    Dynamic
    Compressed
    Reduntant
行格式构成
    记录的额外信息
        变长字段长度列表
        NULL值列表
        记录头信息
            delete_mask 删除标记
            min_rec_mask 标识每层非叶子节点最小记录
            n_owned 表示当前记录拥有的子记录数量
            heap_no 表示当前记录在记录堆的位置信息
            record_type 当前记录类型 0普通记录 1非叶子节点记录 2最小记录 3最大记录
            next_record 下一条记录的相对位置
    记录的真实数据
        row_id
        transaction_id
        roll_pointer
        真实数据